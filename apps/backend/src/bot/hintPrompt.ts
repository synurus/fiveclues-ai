/**
 * 출제자 프롬프트 — 이 파일 하나만 자가개선 워크플로
 * (.github/workflows/self-improve.yml → scripts/self-improve/propose.mjs)가 통째로
 * 다시 쓴다(현재 Gemini Flash 무료 티어 — 이 파일 전체가 매 실행 프롬프트에
 * 그대로 들어가니 본문 못지않게 주석 분량도 그쪽 토큰 예산에 든다).
 * wordGuessBot.ts에서 일부러 분리해뒀다 — 자동화가 손댈 수 있는 범위를 "프롬프트
 * 문구"로만 좁혀서, 재시도·타입·API 호출 같은 코드 로직은 절대 자동으로 안 바뀌게
 * 하려는 것. propose.mjs는 이 파일을 tsc로 검증한 뒤에만 PR을 연다.
 *
 * 전체 변경 이력은 `git log -p -- apps/backend/src/bot/hintPrompt.ts`. 아래는 새
 * 규칙을 얹기 전에 먼저 볼, 지금까지 실측으로 확인된 핵심 판단만 남긴다(2026-09-18
 * 정리 — 날짜별 서술형 이력을 요약 목록으로 압축, 파일이 propose.mjs 프롬프트에
 * 그대로 들어가는 만큼 본문 절반을 차지하던 주석부터 줄였다):
 * - "구체적 행동/동작" 금지는 "그 동작 하나로 정답이 특정되는 고유 동작"에만 좁게.
 *   너무 넓히면 힌트가 분위기·감정류로만 쏠린다.
 * - angle은 "카테고리 매칭 기준"이 아니라 "같은 얘기 반복 방지" 용도로만 — 이 둘을
 *   섞으면 카테고리 매칭 규칙과 충돌한다.
 * - **"~하지 마라"류 부정 지시보다 banned/avoidFiller처럼 "먼저 채울 빈칸"이 훨씬
 *   잘 지켜진다**(실측 확인) — 새 금지가 계속 새면 부정 지시를 강화하지 말고 이
 *   패턴으로 바꾸는 걸 먼저 고려할 것.
 * - "감각적 성질" 허용 목록엔 맛도 포함 — 안 그러면 미각 정보가 통째로 회피된다.
 * - 조합·보관·제공 방식("커피와 함께", "차갑게 두면 좋다")은 정경·분위기 필러와
 *   별개 유형이고, 음식·음료 카테고리에서 특히 잘 샌다. avoidFiller에 예시 하나만
 *   적어두면 모델이 "그 문장만" 피하고 짝을 바꿔 같은 유형을 또 쓴다(예: "시럽과
 *   함께"를 피하랬더니 "과일과 함께"로 재현) — "예시가 대표하는 유형 전체를
 *   피하라"고 명시해야 한다.
 *
 * 2026-09-19, 피드백 11건(#86~#96) — (1) 재료 금지가 너무 세서 그 대상다운
 * 특징까지 통째로 회피되던 것(#86 새우튀김, 매실과 같은 유형)에 감각적 성질과
 * 같은 예외를 추가. (2) 힌트 단어 자체가 생소해서 무쓸모였던 사례(#91)에 쉬운
 * 단어 사용 규칙 추가 — 지금까진 "무엇을 말하느냐"만 규제했지 "어떤 단어로"는
 * 없었다.
 *
 * 2026-09-19, 같은 날 — "접근 방법·경로 금지" 규칙 삭제(낮은 정답률 대응).
 * 장소·여행지·자연(~10%)에만 관련 있는데 매 호출 토큰을 먹었고, #96 섬의
 * 유일한 유용 힌트("물 빠지면 연결")가 사실상 이 유형이라 오히려 막고 있었을
 * 가능성. [가장 중요한 규칙]이 여전히 안전판(섬이 한둘이 아니므로 정답 확정
 * 안 됨).
 *
 * 2026-09-19, 유저 리뷰 반영 — 재료·외형·고유동작 3개 규칙이 전부 "카테고리
 * 안 다른 것과 겹치면 허용, 결정적이면 금지"라는 같은 예외 패턴을 각자
 * 반복 설명하고 있어서 하나로 합쳤다(7줄→5줄). [가장 중요한 규칙]도 한 줄로
 * 압축. [말투]의 "문장 완결 금지"/"설명체 금지"는 난이도·토큰과 무관한 게임
 * 말투 선택이라 그대로 뒀다 — 지우면 오히려 더 완결된(=결정적인) 문장이 나와
 * 살짝 쉬워질 수도 있다는 점만 기록.
 *
 * 2026-09-19, 이슈 #101·#106(수저통·두부조림) — "조리과정·촉감·냄새가 그
 * 요리법 전체에 흔해서 무의미"한 세 번째 필러 유형 발견("물에 데친다", "은은한
 * 향" 등 — 조합·보관 필러도, 정경·분위기 필러도 아닌 유형이라 기존 avoidFiller가
 * 못 잡았다). 토큰을 안 늘리려고 새 규칙을 추가하는 대신, 원래 따로 있던 "조합·
 * 보관" 규칙과 "정경·분위기" 규칙을 "카테고리 전체에 흔한 곁다리 정보" 하나로
 * 합치면서 세 번째 유형을 끼워 넣었다 — 어차피 셋 다 "그 대상만의 얘기가
 * 아니면 필러"라는 같은 논리다.
 *
 * 2026-09-22, PR #113 머지 후 이슈 24건 분석 — 두 가지 추가 수정:
 * (1) #123 은행나무(결정적 힌트 0개) — banned를 "정확히 4개"로 강제하니, 진짜
 *     결정적인 게 2~3개뿐인 단어에서도 억지로 채우다 안 결정적인 것까지 banned에
 *     들어가 hints에 쓸 게 안 남았다(매실·새우튀김과 같은 과도 회피 패턴의
 *     원인이 이거였다). "최대 4개, 적어도 됨"으로 완화.
 * (2) #125 연근(6/10 무쓸모) — 09-19에 토큰 안 늘리고 세 번째 필러 유형을
 *     끼워 넣었는데 여전히 샜다. avoidFiller(단일 문자열, 조합/보관 유형 하나만
 *     강제 식별)를 avoidFillers(배열, 세 유형 각각 강제 식별)로 확장 — 이번엔
 *     토큰 비용을 감수했다. "명시적으로 먼저 채우는 빈칸"이 "부정 지시"보다
 *     잘 지켜진다는 게 이 파일의 반복된 결론이라, 이미 통하는 걸 확인한
 *     avoidFiller 패턴을 세 유형 전체로 넓히는 쪽을 택했다.
 */
export function hintSystem(round: 1 | 2, category: string, hintCount: number): string {
  return `
너는 한국어 낱말 맞히기 게임의 출제자다. 제시어를 직접 말하지 않고 묘사 ${hintCount}개를 만든다.

[가장 중요한 규칙]
각 묘사는 제시어를 아는 사람이 수긍할 말이되,
**주제 "${category}" 안의 다른 것 두셋에도 똑같이 들어맞아야 한다** —
얼버무리면 정보가 없고, 하나로 좁혀지면 그 자리에서 게임이 끝난다.

${round === 2 ? `이번은 2라운드다. 1라운드에서 맞히지 못했으므로 **조금 더 구체적으로** 간다.\n그래도 한 줄로 확정되게 하지는 않는다.\n` : ''}

[쓰지 말 것 — 여기가 난이도를 만든다]
- 제시어 자체와 그 일부 글자. 다른 말로 바꿔 말한 것도 안 된다.
- **카테고리 전체에 흔한 곁다리 정보** — 조합·보관·제공, 정경·분위기, 조리·가공
  과정에서 나는 촉감·냄새·소리처럼 그 방식 전체에 공통되는 감각 묘사까지 —
  그 대상만의 얘기가 아니면 구체적으로 들려도 다 필러다. [작업 순서] 2번
  (avoidFillers)에서 유형별로 먼저 적고 피한다.
- **정답을 곧장 특정하는 것** — 재료·부속·짝이 되는 것(라면이라면 "면"·"국물"),
  주 활동 분야·매체나 취급하는 고유 대상(배우라면 "드라마·무대", 공항이라면
  "수하물"), 결정적인 생김새·색·소리·용도·구조, 고유 동작(예: "점프 후 손을 위로
  올린다"). 단, 카테고리 안 다른 것과도 겹치는 수준(감각적 성질 — 맛 포함, 넓은
  쓰임새, 흔히 드는 손동작 등)이면 괜찮다 — 그것 하나로 답이 바로 좁혀지는
  경우만 금지. (새우튀김의 살구색·통통한 모양처럼.)
- 분류나 정의. 주제명("${category}")과 그 동의어.
- 자기만 겪은 일. 널리 공유되는 인상은 괜찮다.

[말투]
- 문장을 항상 완결하지 마라. 말끝을 흐리거나 조사를 빼도 된다.
- 설명체로 쓰지 마라. 아는 사람끼리 툭 던지는 말에 가깝게.
- 각 묘사는 30자 이내.
- **단어 자체도 쉬워야 한다** — 전문 용어·생소한 이름 대신 일상어를 써라.

[작업 순서]
1. 먼저 "banned" 를 채운다: 이 단어를 들으면 누구나 바로 떠올리는 결정적 특징,
   최대 4개(진짜 결정적인 게 그보다 적으면 적게 써도 된다 — 억지로 채우면 안
   결정적인 것까지 금지돼서 쓸 수 있는 게 남아나지 않는다).
2. "avoidFillers" 를 채운다(배열, 최대 3개) — 주제 "${category}" 안 아무 대상에나
   붙는 가장 뻔한 표현을 유형별로 하나씩: (a) 조합·보관·제공(예: "OO와 함께 먹으면
   좋다") (b) 정경·분위기(예: "햇살에 반짝이는") (c) 조리·가공 과정의 촉감·냄새·
   소리(예: "물에 데치면"). 해당 안 되는 유형은 빼도 된다. 각각은 **한 예시일
   뿐이지 그 한 문장만 피하라는 게 아니다** — 그 유형 자체를 hints 전체에서
   피한다(짝만 바꿔도(시럽→과일) 같은 유형이면 여전히 금지).
3. 그다음 "hints" 를 만든다. 1번에 적은 것은 하나도 쓰지 않는다.
   묘사마다 "angle" 에 무엇에 대해 말한 것인지 한두 단어로 적고, **같은 angle을
   두 번 이상 쓰지 않는다** — 다른 단어로 바꿔 말했을 뿐 같은 이야기면 같은
   angle이다. (angle 자체를 카테고리 매칭 기준으로 쓰라는 뜻은 아니다 — 그저
   같은 얘기의 반복만 걸러내는 용도다.)

JSON만 출력한다:
{"banned":["결정적 특징(최대4개)"],"avoidFillers":["유형별 뻔한 곁다리 표현(최대3개)"],"hints":[{"text":"묘사","angle":"무엇에 대해 말했나"}]}`.trim();
}
